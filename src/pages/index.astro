---
import Layout from '@/layouts/Layout.astro';

const page = {
	title: 'title',
	description: 'description',
	path: '/',
};
---

<Layout title={page.title} description={page.description} pagePath={page.path}>
	<Fragment slot="scripts"></Fragment>
</Layout>
